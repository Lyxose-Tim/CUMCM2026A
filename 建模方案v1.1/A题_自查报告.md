# A 题建模方案 v1.2（定点返修）自查报告

| 项目 | 内容 |
|---|---|
| 对象 | **本轮实提交的五份文件**：《A题_建模方案.md》v1.2（定点返修）、《A题_决策日志.md》v1.2（定点返修）、`A题_config.yaml`、`config_check.py`、本报告 |
| 依据 | 《A题_定点返修.md》PATCH-01~08（用户授权同步修补五份文件；不是重新设计、不新增阶段计划、不启动完整数值求解） |
| 日期 | 2026-09-12 |
| 证据口径 | 「规格已修订」= 文档/配置就地改到位；「配置/轻量测试实际通过」= 本轮代码实跑（config 正/负例、`-O`、props 代入、增广态收支单测）；「求解器已实现待正式执行」= 代码已实现但正式 PDE 场解/收敛验收未跑；「正式数值/输出未执行」= result1–4、正式 $t^*$ 未生成；「模板实物核验」= 本轮代码工程用 openpyxl 实读附件3；「待人工核验」= 参赛队核对（本轮无） |
| 代码工程 | `src/drymodel`（config/props/data_io/grid/operators/solver_be/solver_bdf/criterion/postprocess/writers/verify/sensitivity/plots/run_all），随附本仓库；测试 `tests/`（71 例通过） |

> **本轮完成的判断**：已通过的主模型未被改坏；返修文档指出的前后矛盾与配套纰漏得到同步修补；已有轻量检查实际可执行并记录真实结果；未运行的数值验证与未核实来源没有被写成通过。**未把整个赛题数值求解与论文交付全部完成。**

## 1. PATCH-01~08 对照表（四·交付要求）

| PATCH | 实际改动文件与位置 | 当前规则 | 已执行证据 | 尚待验证项 |
|---|---|---|---|---|
| 01 五份同步 + 参数分类 | config.yaml 首页/参数注释；方案首页；日志首页；本报告 | 五份统一 v1.2（定点返修）；题给物理参数带 unit/source，数值/情景参数简洁；删「全部三标签」声明 | 五份版本号一致；config 加载通过 | — |
| 02 消除 D12 循环 + Q1 终点 | 方案 §0.2/§6.0.2/§7/§9.4/V-11；config `criterion.applies_to`、`production.note` | D12 依适用于该问的数值检验选配置；V-8/V-9 导出后验收（非前置）；Q1 仅解 1800 s、不适用达标事件/续算；配置变化→受影响轨迹失效重积分 | config `applies_to=[q23,q4]`、`per_question.q1.applies_event=false` 校验通过；代码 `run_q1` 无事件/续算 | 正式续算与导出未执行 |
| 03 按误差选网格 | 方案 §6.0.4/§6.1/§6.3/§9.2/§9.7、D12；config `numerics.per_question` | 网格按各问输出误差选定，删 N≥800/Q1 N≥1600 无条件写法；空间误差靠空间加密、时间误差靠容差/步长分别核查；Q4 网格独立验收 | config `per_question.final_N=null` 校验（未授权不填）；探针 E20/E17 分空间/时间已在 §6.0.4 | 最终网格待正式误差检查登记 |
| 04 检查器实拒错误 | **`config_check.py` 重写**；config 新增分问/求积字段；方案 §9.3/§9.6 A-1 | 显式异常/`-O` 不失效；拒绝九类变异（八类+负半径）+ NaN/类型/布尔/负容差；校验说明式与求积/输出终点；可选实测求解器 props | **本轮实跑：正例通过；八类变异+负半径 9/9 被拒；非有限值/类型/负容差 3/3 被拒；props 求解器代入一致 OK**（见 §2） | 生产 config_id/验证记录待正式验收 |
| 05 W/m 单位 + BDF 增广态 | config `acceptance.energy_residual`（去 L）/`numerics.bdf.atol_I`/`augmented_flux_state`；方案 §9.1/9.2、V-4a/b/c；代码 operators/solver_bdf/verify | 能量残差 W/m（`2πR(t)h·max(ΔT_ref,1K)`，无 L）；BDF 累计通量增广态 I（2n+1，İ=(2h_m/R)(C_N−C_env)，事件仅 C），归一化 $\bar C-\bar C_0+I$；V-4b 分 BE/BDF | **增广态代码已实现并自查：固定域 $|\bar C-\bar C_0+I|$≈1.1e-15、Q4 动域≈1.0e-15；单测通过** | V-4c 正式能量残差、V-4b 加密对照未正式跑 |
| 06 缩减必做 | 方案 §8.4/§8.6/§9.4/§9.7/§10、A-8、H01/H12；config `criterion.t_safe`；日志 §8.5/H06 | S7/S8/V-14、S9 诊断图、t_safe 降级为可选，不入必做全项通过；删 A-8「8.4 全部运行」笼统写法；潜热仅一句局限；D05-B 移出必做门槛 | config `t_safe.enabled=false`；代码 `Detection.t_safe` 可选、无 `max(...,t_safe)` | 端面/潜热可选项按需运行 |
| 07 证据状态如实 | 方案 §2/§9.2/§10；日志 §11.7；本报告 | 附件3 模板改「本轮 openpyxl 实读（可追溯）」，分开 result3/result4 表头（result4 末列=药材表面，非 2）；§9.2 配置表「必须检查项目/待验证」；外部探针与本轮执行分列 | 见 §3 实读结构摘要 | 正式数值/论文文本、S02/S08/S09 出处待人工核验 |
| 08 物理表述 + 输出细节 | 方案 H04/H12/§5/§6/§9.5；日志 D06/H04 | 删「ρ 不是密度/b 不作真实密度」，改 ρ、cp、k 保留题给经验物性作用、热方程用 b=ρcp；ρ_s0 仅换算、按问题物性组；表 1/2 明确 7 时刻；§6.3 最后有效时刻区分几何交界 vs 文件采样行 | 代码 `run_q1` 表 1/2 时刻=[100,300,600,900,1200,1500,1800]（单测/实跑确认）；`postprocess` 由 R(t) 判域 | 正式表值未生成 |

## 2. 配置检查器真实结果（PATCH-04）

**运行**：`python 建模方案v1.1/config_check.py 建模方案v1.1/A题_config.yaml`（及 `python -O`）。

| 用例 | 结果 |
|---|---|
| 正例（原始合法配置） | **通过**（version 1.2、interface harmonic、approved False） |
| 八类非法变异 + 负半径（`geometry.R0=-0.02`） | **9/9 被拒**（`python` 与 `python -O` 一致；显式 `ConfigError`，不依赖 `assert`） |
| 非有限值 / 类型 / 负容差（布尔冒充 `N_default`、`rtol=NaN`、`atol_C<0`） | **3/3 被拒** |
| 求解器物性实测（`check_solver_props`：`drymodel.props` 的 D_app2/props_app3/props_app4 正 C、K 温度代入） | **一致 OK**（代码项目 `tests/test_props.py` 亦覆盖） |

八类变异：Q1 密度 −820、Q2/Q3 的 D 前因子 2.4e3、Q4 的 D 改 0、`N_default` −200、BDF rtol 负、result2 终点 3h、`energy_form` enthalpy_advective、`q4.axial` shrinking_L。**「配置解析通过 ≠ 公式—配置一致，更 ≠ 模型验证通过」；校验说明文字 ≠ 求解器物性函数已过测试（后者由 props 实测覆盖）。**

## 3. 附件3 模板实读结构摘要（PATCH-07，可追溯）

**读取方法**：本轮代码工程 `openpyxl.load_workbook(..., read_only=True)` 读取 `附件/附件3/result1-4.xlsx`（2026-09-12）。

| 文件 | 工作表 | A1 | 表头（缩略示例，含 `…` 占位） | A 列 |
|---|---|---|---|---|
| result1 / result2 | 温度、水分浓度 | `时间\到药材中心的距离` | 0, 0.1, 0.2, `…`, 2 | 1, 2, 3, `…` |
| result3 | Sheet1 | 同上 | 0, 0.1, 0.2, `…`, **2** | 60, 120, 180, `…` |
| result4 | Sheet1 | 同上 | 0, 0.1, 0.2, `…`, **「药材表面」**（末列非「2」） | 60, 120, 180, `…` |

PDF 仅可直接确定：result1/2 工作表名、各文件 A 列时间单位 s、第一行距离单位 cm。A1 逐字文本、result3/4 的 Sheet1 名、无合并单元格、首行步距按上述实读记录，不单凭 PDF 认定。

## 4. 执行状态（D12 已授权 + 正式生产完成）

| 类别 | 项 |
|---|---|
| **D12 已授权并正式生产** | `production.approved=true`、config_id=`D12-20260912-N1600.800.800-integral8pt-bdf1e-8`、final_N=1600/800/800（依 reports/D12_evidence.md 场点误差选定）；正式 result1–4 + 表 1–6 已导出（outputs/），V-8/V-9 全过（见 reports/production_receipt.md）；未舍入 $t^*_3$=57.474027 h、$t^*_4$=51.092029 h |
| 已实现并实测通过 | config 正/负例与 `-O`、两检查器一致、props 代入、V-1/V-2、V-3 分对象（含 Q4 r=1.2 cm）、V-4a/b(BE/BDF)/c、V-5 包络、V-6、V-10、V-11、S10；D12 证据全部正式采样行 C/T 场点误差（reports/D12_evidence.md，**本轮自执行，与外部审计探针分列**） |
| 端面校核 V-15 | **已完成**（§8.2 线性辅助端面增量，覆盖 Q1–Q4 全部表点含 Q2 0.5–3 h）：热 max\|ΔT\|=9.1013e-4 ℃@1.5h、质 max\|ΔC\|=1.1281e-5@42h，均 <0.1/0.005 通过；截断收敛证据见 reports/V15_end_effect.md |
| 待人工核验 | S01 原文 PDF、S02/S08 章节、S09 教材页码、实际工具/模型版本、论文全部数值与文本 |
| 已降级（可选/历史） | 潜热 D05-B/V-14/S8、ρ_s0 灵敏度 S7、S9 诊断图、t_safe、端面非线性/二维校核——不入必做全项通过条件 |

## 5. 文档一致性核对（不只搜关键词，核对表格/配置实际含义）

| 项 | 结果 |
|---|---|
| 五份版本号统一 v1.2（定点返修） | 是 |
| D12 循环消除（V-8/V-9 非前置）；Q1 不适用达标事件 | 是（方案 §0.2/§6.0.2/§7/§9.4/V-11、config `applies_to`） |
| 「或首段细步」并列删除；空间/时间误差分别核查 | 是（方案 §9.7、§6.0.4） |
| 「b…不作真实密度」「ρ 不是密度」混淆句删除 | 是（方案/日志 H04、D06） |
| 表 1/2 明确 7 时刻（非「100…1800」） | 是（方案 §6.1/§9.5/R05、代码 `run_q1`） |
| result4 末列「药材表面」（非「2」）；result3/4 表头分开 | 是（方案 §2） |
| 「8.4 全部运行」「V-1~V-15/S1~S7 全部尚待执行」笼统写法替换为适用项 | 是（方案 A-8、§8.6） |
| 能量残差 W/m（无 L） | 是（config `acceptance`、方案 V-4c/H04） |
| 未运行项如实标注、外部探针与本轮执行分列、`approved` 未改、正式 $t^*$ 未填 | 是 |

> 说明：不因检索不到几个关键词即宣布全文件一致；上表为核对相关表格与配置实际含义后的结论。历史修订记录（§11.1–11.6 于日志）保留为归档事实，不改写。
