# A 题建模方案 v1.2（GitHub 候选阶段审计返修）自查报告

| 项目 | 内容 |
|---|---|
| 对象 | 工作区 `src/drymodel`、`tests`、两份同步 YAML、方案/日志/README、自查与生成的候选报告/图 |
| 依据 | 《A题 GitHub 候选阶段审计》（对象提交 `252c639f0c1e8949804c12ab721b3fc84a4681e7`）F01–F07 与定点要求 1–10；不重建主模型 |
| 日期 | 2026-09-12 |
| 证据口径 | `pass/fail/partial/not_run` 全由 `reports/verification.json` 的实测 metric/limit/config/coverage 生成；外部审计数字只作比对，不冒称本轮执行。候选值不是正式答案；`production.approved=false`，正式 result1–4 未生成 |
| 代码工程 | `src/drymodel` 全链路；测试 `tests/` 全部通过（精确收集数以本轮 pytest 日志为准）；候选全量验证环境与版本见 `reports/status.md` |

> **本轮完成的判断**：F01–F07 的代码缺陷均已修复并有回归；Q1/Q23/Q4 的 D12 前适用数值检查已按 N=800 候选、N=1600 空间参考及独立时间加密实际运行并通过。BE `dt=1 s` 粗步 V-4b 仍失败且原样保留，`dt=0.25 s` 通过。V-8/V-9 因正式文件未生成而仍未执行；生产编排仍未实现；D12 仍待用户授权。**未把候选通过改写成正式答案或论文交付完成。**

## 1. GitHub 候选阶段审计 F01–F07 闭环

| 审计项 | 修复与回归 | 本轮实测状态 |
|---|---|---|
| F01 状态与覆盖 | 统一结构化记录；报告只渲染 JSON；门控逐问核对必检 ID；失败/局部/未运行不提升 | D12 前适用项全部通过；BE 粗步失败、图8局部证据、V-8/V-9 未运行分别保留 |
| F02 BE/BDF 收支 | BE 记录每个真实接受子步并传播失败；热残差取相邻真实状态/Δt；BDF 候选启用 I，独立 Simpson 自适应加密并与时间加密 I 交叉 | BE V-4b：dt=1 s 为 1.6426412e-4（失败），dt=0.25 s 为 4.1066774e-5（通过），阶 0.999987；BDF Q23/Q4 独立差 4.14e-8/4.48e-8（通过） |
| F03 配置一致性 | `src/drymodel/config_check.py` 唯一权威，方案入口薄转调；严格类型/有限性/输出/授权证据；每次运行保存完整快照和摘要；断点、求积点、情景幅度均实际接线 | 两 YAML 逐字一致；当前候选摘要 Q1/Q23/Q4=`028adb8ba47d8561`/`54cc2666b51a991f`/`c11eb5b2c61c4c53` |
| F04 表5 | 改为时间 + 0/0.5/1/1.5/2 cm 共六列，Cmax 单列诊断，6 h 与末行容差去重 | 结构与去重回归通过 |
| F05 同源有界轨迹 | 事件段与续算段严格拼接并保留 I；越界/非有限/失败/未到终点均报错；秒/分钟/表值共用该轨迹 | Q23/Q4 严格分钟值 0.1499902434/0.1499852689，事件后检查通过 |
| F06 工作簿终检器 | 流式逐项核对表头、真实整数时间、数值/格式、域内外、表面列、首末行，并实现 result2/3 共同范围逐格检查 | 正负例回归通过；正式 V-8/V-9 因未导出文件仍记 `not_run` |
| F07 灵敏度误差 | Q23 S1–S6 每个候选/加密情景分别运行；验收门槛、实测误差与合成不确定性分栏，配置摘要逐行保存 | 22 条轨迹已实测；Q1/Q4 未扫描并明确记录 |

全场最差空间差：Q1 1.8991e-4（t=1 s，r=2.0 cm）；Q23 1.5488e-4（t=1 s，r=2.0 cm）；Q4 2.1941e-4（t=124620 s，r=1.2 cm）。完整位置、温度/平均值/通量/事件/求积指标见 `reports/verification.json`。

## 2. PATCH-01~08 历史对照表（保留原定点返修脉络）

| PATCH | 实际改动文件与位置 | 当前规则 | 已执行证据 | 尚待验证项 |
|---|---|---|---|---|
| 01 五份同步 + 参数分类 | config.yaml 首页/参数注释；方案首页；日志首页；本报告 | 五份统一 v1.2（定点返修）；题给物理参数带 unit/source，数值/情景参数简洁；删「全部三标签」声明 | 五份版本号一致；config 加载通过 | — |
| 02 消除 D12 循环 + Q1 终点 | 方案 §0.2/§6.0.2/§7/§9.4/V-11；config `criterion.applies_to`、`production.note` | D12 依适用于该问的数值检验选配置；V-8/V-9 导出后验收（非前置）；Q1 仅解 1800 s、不适用达标事件/续算；配置变化→受影响轨迹失效重积分 | config `applies_to=[q23,q4]`、`per_question.q1.applies_event=false` 校验通过；代码 `run_q1` 无事件/续算 | 正式续算与导出未执行 |
| 03 按误差选网格 | 方案 §6.0.4/§6.1/§6.3/§9.2/§9.7、D12；config `numerics.per_question` | 网格按各问输出误差选定，删 N≥800/Q1 N≥1600 无条件写法；空间误差靠空间加密、时间误差靠容差/步长分别核查；Q4 网格独立验收 | 本轮 N=800/1600 与独立时间加密覆盖全部输出行/表点/平均量/通量；三问均低于门槛 | `final_*` 仍为 null，待 D12 授权后登记，不由通过状态自动写入 |
| 04 检查器实拒错误 | **`src/drymodel/config_check.py` 唯一权威**；方案目录入口仅转调；config 新增分问/求积字段 | 显式异常/`-O` 不失效；关键类型、有限性、范围、完整输出规则、授权参数/摘要/记录均校验；求积和情景参数实际接线 | 审计点名变异及新增负例全部纳入当前完整 pytest；两 YAML 逐字一致；每次运行配置摘要入 JSON | 正式生产仍须在入口核对所授权摘要与验证记录；当前入口未实现 |
| 05 W/m 单位 + BDF 增广态 | config `acceptance.energy_residual`（去 L）/`numerics.bdf.atol_I`/`augmented_flux_state`；方案 §9.1/9.2、V-4a/b/c；代码 operators/solver_bdf/verify | 能量残差 W/m（`2πR(t)h·max(ΔT_ref,1K)`，无 L）；BDF 累计通量增广态 I（2n+1，İ=(2h_m/R)(C_N−C_env)，事件仅 C），归一化 $\bar C-\bar C_0+I$；V-4b 分 BE/BDF | Q23/Q4 BDF 增广残差约 1.2e-15/1.9e-15；独立求积及时间加密均通过；BE 0.5 s 热残差路径通过 | BE 1 s 独立梯形粗步仍失败并如实保留；0.25 s 通过 |
| 06 缩减必做 | 方案 §8.4/§8.6/§9.4/§9.7/§10、A-8、H01/H12；config `criterion.t_safe`；日志 §8.5/H06 | S7/S8/V-14、S9 诊断图、t_safe 降级为可选，不入必做全项通过；删 A-8「8.4 全部运行」笼统写法；潜热仅一句局限；D05-B 移出必做门槛 | config `t_safe.enabled=false`；代码 `Detection.t_safe` 可选、无 `max(...,t_safe)` | 端面/潜热可选项按需运行 |
| 07 证据状态如实 | 方案 §2/§9.2/§10；日志 §11.7；本报告 | 附件3 模板改「本轮 openpyxl 实读（可追溯）」，分开 result3/result4 表头（result4 末列=药材表面，非 2）；§9.2 配置表「必须检查项目/待验证」；外部探针与本轮执行分列 | 见 §3 实读结构摘要 | 正式数值/论文文本、S02/S08/S09 出处待人工核验 |
| 08 物理表述 + 输出细节 | 方案 H04/H12/§5/§6/§9.5；日志 D06/H04 | 删「ρ 不是密度/b 不作真实密度」，改 ρ、cp、k 保留题给经验物性作用、热方程用 b=ρcp；ρ_s0 仅换算、按问题物性组；表 1/2 明确 7 时刻；§6.3 最后有效时刻区分几何交界 vs 文件采样行 | 代码 `run_q1` 表 1/2 时刻=[100,300,600,900,1200,1500,1800]（单测/实跑确认）；`postprocess` 由 R(t) 判域 | 正式表值未生成 |

## 3. 配置检查器真实结果（PATCH-04；本轮已扩展）

**运行**：`python 建模方案v1.1/config_check.py 建模方案v1.1/A题_config.yaml`（及 `python -O`）。

| 用例 | 结果 |
|---|---|
| 正例（当前合法配置） | **通过**（version 1.2；分问候选均为 integral/BDF；approved False） |
| 原九类非法变异及审计新增变异 | **全部被拒**；覆盖 `atol_I<0`、`atol_C=∞/bool`、`atol_T_K` 字符串、负 `max_step_after_s/tol_dC`、非整数迭代/减步次数、NaN 情景幅度、错误 result1 时间规则等 |
| 授权字段负例 | approved=true 但缺 final_*、错误摘要格式、错误验证记录路径、未验证 N/求积点数均被拒；合法具体配置可解析为生产有效快照 |
| 普通与优化模式 | 方案目录薄入口与权威函数同源；`python -O` 仍以显式 `ConfigError` 拒绝非法配置 |
| 求解器物性实测（`tests/test_props.py`：`drymodel.props` 的 D_app2/props_app3/props_app4 正 C、K 温度代入） | **一致 OK**（自动化测试覆盖） |

八类变异：Q1 密度 −820、Q2/Q3 的 D 前因子 2.4e3、Q4 的 D 改 0、`N_default` −200、BDF rtol 负、result2 终点 3h、`energy_form` enthalpy_advective、`q4.axial` shrinking_L。**「配置解析通过 ≠ 公式—配置一致，更 ≠ 模型验证通过」；校验说明文字 ≠ 求解器物性函数已过测试（后者由 props 实测覆盖）。**

## 4. 附件3 模板实读结构摘要（PATCH-07，可追溯）

**读取方法**：本轮代码工程 `openpyxl.load_workbook(..., read_only=True)` 读取 `附件/附件3/result1-4.xlsx`（2026-09-12）。

| 文件 | 工作表 | A1 | 表头（缩略示例，含 `…` 占位） | A 列 |
|---|---|---|---|---|
| result1 / result2 | 温度、水分浓度 | `时间\到药材中心的距离` | 0, 0.1, 0.2, `…`, 2 | 1, 2, 3, `…` |
| result3 | Sheet1 | 同上 | 0, 0.1, 0.2, `…`, **2** | 60, 120, 180, `…` |
| result4 | Sheet1 | 同上 | 0, 0.1, 0.2, `…`, **「药材表面」**（末列非「2」） | 60, 120, 180, `…` |

PDF 仅可直接确定：result1/2 工作表名、各文件 A 列时间单位 s、第一行距离单位 cm。A1 逐字文本、result3/4 的 Sheet1 名、无合并单元格、首行步距按上述实读记录，不单凭 PDF 认定。

## 5. 已修复并实测 / 仍失败 / 仍未执行 / 待授权

| 类别 | 项 |
|---|---|
| 已修复并实测通过 | F01–F07 回归；权威配置/镜像/有效快照；Q1/Q23/Q4 各自适用的 D12 前数值门控；Q23 S1–S6 实测时间加密；候选 $t^*_3=57.47402726$ h、$t^*_4=51.09202937$ h（均未授权、非正式答案） |
| 仍失败 | BE V-4b 验证基线 `dt=1 s` 为 1.6426412e-4 >1e-4；同一检查 `dt=0.25 s` 为 4.1066774e-5 并通过，粗细步阶约 1。粗步失败不删除、不提升 |
| 局部通过 | 图8的 Q23 harmonic/integral 时长趋势；只作为界面趋势证据，不替代三问全场 V-3/V-11 |
| 仍未执行 | 生产续算与正式导出（代码仍 `NotImplementedError`）；正式 result1–4；导出后的 V-8 result2/3 跨文件检查与 V-9 工作簿结构读回 |
| 待用户授权 | D12 生产数值配置（界面系数、网格、时间格式）；`production.approved=false` 保持 |
| 待人工核验 | S01 原文 PDF、S02/S08 章节、S09 教材页码、实际工具/模型版本、论文全部数值与文本 |
| 已降级（可选/历史） | 潜热 D05-B/V-14/S8、ρ_s0 灵敏度 S7、S9 诊断图、t_safe、端面非线性/二维校核——不入必做全项通过条件 |

## 6. 文档一致性核对（不只搜关键词，核对表格/配置实际含义）

| 项 | 结果 |
|---|---|
| 五份版本号统一 v1.2（定点返修） | 是 |
| D12 循环消除（V-8/V-9 非前置）；Q1 不适用达标事件 | 是（方案 §0.2/§6.0.2/§7/§9.4/V-11、config `applies_to`） |
| 「或首段细步」并列删除；空间/时间误差分别核查 | 是（方案 §9.7、§6.0.4） |
| 「b…不作真实密度」「ρ 不是密度」混淆句删除 | 是（方案/日志 H04、D06） |
| 表 1/2 明确 7 时刻（非「100…1800」） | 是（方案 §6.1/§9.5/R05、代码 `run_q1`） |
| result4 末列「药材表面」（非「2」）；result3/4 表头分开 | 是（方案 §2） |
| 「8.4 全部运行」「V-1~V-15/S1~S7 全部尚待执行」笼统写法替换为适用项 | 是（方案 A-8、§8.6） |
| 能量残差 W/m（无 L） | 是（config `acceptance`、方案 V-4c/H04） |
| 未运行项如实标注、外部探针与本轮执行分列、`approved` 未改、正式 $t^*$ 未填 | 是 |

> 说明：不因检索不到几个关键词即宣布全文件一致；上表为核对相关表格与配置实际含义后的结论。历史修订记录（§11.1–11.6 于日志）保留为归档事实，不改写。
