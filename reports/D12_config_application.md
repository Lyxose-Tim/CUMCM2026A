# D12 生产配置申请（供审批；approved 仍为 false，未生成正式 result1–4）

> 由各问实际误差选定最终 N（生产配置 vs 更精细对照，同 BDF rtol）。空间/独立时间/8-16 点界面对照分别登记；不以 t* 收敛替代场点验收。

## 拟定配置（待审批写入 config production.approved=true + per_question.final_N + config_id）

| 问 | 界面 | 求积点 | BDF rtol/atol | max_step | 对照 N | 拟定最终 N | 场点验收 |
|---|---|---|---|---|---|---|---|
| Q1(附录2) | integral | 8(16对照) | 1e-08/1e-11,1e-08 | 60/600s | (1600, 3200) | **1600** | 通过 |
| Q2/Q3(附录3) | integral | 8(16对照) | 1e-08/1e-11,1e-08 | 60/600s | (800, 1600) | **800** | 通过 |
| Q4(附录4) | integral | 8(16对照) | 1e-08/1e-11,1e-08 | 60/600s | (800, 1600) | **800** | 通过 |

## Q1 场点误差（result1 全部 1 s 行×21 列）

- max|ΔC| = 4.693e-05（t=1s, r=2.0cm），门槛 5e-04
- max|ΔT| = 2.232e-04 °C（t=1500s, r=2.0cm），门槛 1e-02
- 结论：满足 → 最终 N=1600

## Q2/Q3 场点误差（早期 1 s 行 + 60 s 全部行 + 通量/均量 + t*）

- max|ΔC| = 1.549e-04（t=1s, r=2.0cm），门槛 5e-04
- 表面通量差 2.686e-11；C̄ 差 2.999e-07；Δt*(N对照) 2.219e-05 h
- 结论：满足 → 最终 N=800

## Q4 场点误差（全部固定厘米列 + 表面 + r=1.2 cm，60 s 全部行）

- max|Δ| = 2.194e-04（t=124620s, 列=1.2），门槛 5e-04
- **r=1.2 cm 专列 max|Δ| = 2.194e-04**（独立登记，不以 t* 替代）；Δt*(N对照) 2.026e-05 h
- 结论：满足 → 最终 N=800

## 独立时间加密 与 8/16 点界面求积对照

- Q23 时间对照（N=800，rtol 1e-8→1e-10）：Δt* = 8.561e-08 h
- Q23 界面 8 vs 16 点（N=800）：Δt* = 3.369e-07 h
- Q4 界面 8 vs 16 点（N=800）：Δt* = 6.237e-07 h

## 审批说明

- 审批通过后：置 `production.approved=true`、填 `per_question.{q1,q23,q4}.final_N` 与 `production.config_id`，再运行 `python -m drymodel.run_all --produce` 生成官方 result1–4 + 表 1–6 并跑 V-8/V-9。
- 正式文件缺席不作否决理由；本申请仅提交配置与验收证据，approved 保持 false。