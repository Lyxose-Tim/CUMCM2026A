# A题候选阶段审计返修报告

- 基线：`252c639f0c1e8949804c12ab721b3fc84a4681e7`
- 分支：`codex/audit-remediation-252c639`
- 完整候选重跑：2026-09-12 02:44:26（Python 3.13.9、NumPy 2.3.5、SciPy 1.16.3、openpyxl 3.1.5、PyYAML 6.0.3）

## 结论

- F01–F07 已按审计要求完成代码修复并增加回归测试。
- 当前机器可读门控为 `ready_for_D12_authorization=true`，表示候选适用数值检查已通过、可以提交用户考虑授权。
- `production.approved=false` 保持不变；D12 尚未授权，生产编排仍未实现，正式 `result1–4` 仍未生成。
- 诊断性 V-4b/BE `dt=1 s` 粗步仍按实测保留失败；`dt=0.25 s` 通过且呈一阶收敛。候选长时方案使用 BDF，其独立质量收支检查通过。

机器可读事实源为 `reports/verification.json` 与 `reports/sensitivity.json`；Markdown 报告仅由这些结构化状态渲染，不独立写死“通过”。

## F01–F07 修复映射

| 审计项 | 修复 | 回归/证据 |
|---|---|---|
| F01 状态写死、覆盖不足 | `run_all.py` 与 `verify.py` 统一输出 `status/metric/limit/config/coverage/worst_location`；缺失项记 `not_run`；按问门控；V-1/2 空间与时间分栏；V-3 覆盖逐秒/分钟、表点、通量、平均量、全场极值、Q4 固定厘米位置、阈值和独立时间加密；V-5/6/11 纳入主报告 | `tests/test_run_all.py`；`reports/verification.json`、`reports/verification.md`、`reports/status.md` |
| F02 BE 减步/失败/热残差 | `solver_be.py` 保存每个实际接受子步的起止时刻、真实步长和状态；失败立即传播；质量通量按真实子步加权；热残差使用相邻真实状态和真实 `dt`；动域使用 `R(t)` 与一致面积因子 | `tests/test_solvers.py`、`tests/test_balances.py`；V-4a/b/c 记录 |
| F03 检查器与运行配置分叉 | `src/drymodel/config_check.py` 成为唯一权威检查器，方案目录入口仅转调；补齐类型、有限性、范围、输出规则和生产证据校验；删除已废弃且不生效的全局 `numerics.interface`；每问生成不可变有效配置与摘要，N、界面、求积点、积分器、容差、步长、断点和情景幅度均接入实际求解 | `tests/test_config_check.py`、`tests/test_data_io.py`、`tests/test_runners.py`；两份 YAML SHA-256 相同；普通与 `python -O` 校验均通过 |
| F04 表5列结构 | 表5改为时间加 `r=0/0.5/1/1.5/2 cm` 共6列；`Cmax` 单列诊断；表5/6 的整6 h行与 `t*` 行去重 | `tests/test_runners.py` |
| F05 越界外推、轨迹不同源 | BDF 稠密输出拒绝积分区间外和非有限查询；事件段与600 s续算段拼接为有界连续轨迹，保留累计 I；事件、表格和逐秒/分钟输出同源；续算失败或未到终点显式报错 | `tests/test_solvers.py`、`tests/test_runners.py` |
| F06 V-8/V-9 虚检 | 工作簿流式逐行核对工作表、A1、逐项表头、整数连续时间、有限数值、`0.0000` 格式、域内/域外、表面列、末行和行数上限；实现 result2/3 共同时间/位置舍入值逐格比较 | `tests/test_writers.py`；正式文件未生成，因此 V-8/V-9 状态仍为 `not_run` |
| F07 灵敏度误差口径 | 验收门槛与实测数值误差分离；基线和每个情景均作候选/加密对照，以两次求解合成不确定性及方向一致性判断可分辨；明确只运行 Q23 S1–S6，Q1/Q4 未运行 | `tests/test_sensitivity.py`；`reports/sensitivity.json`、`reports/sensitivity.md` |

## 实际生效候选配置

| 问题 | N | 界面/求积 | 积分器 | 增广 I | 配置摘要 |
|---|---:|---|---|---|---|
| Q1 | 800 | integral / 8点 | BDF | 否 | `028adb8ba47d8561` |
| Q2/Q3 | 800 | integral / 8点 | BDF | 是 | `54cc2666b51a991f` |
| Q4 | 800 | integral / 8点 | BDF | 是 | `c11eb5b2c61c4c53` |

候选 BDF：`rtol=1e-8`、`atol_C=1e-11`、`atol_T=1e-8 K`、`atol_I=1e-11`，数据段/后段 `max_step=60/600 s`，在附件断点重启。独立时间参考使用 N=1600、`rtol=1e-10`、`atol_C=1e-13`、`atol_T=1e-10 K`、`atol_I=1e-13`、`max_step=15/150 s`；界面求积另用16点对照。

## 主要实测误差

验收限：`max ΔC ≤ 5e-4`、`max ΔT ≤ 0.01 °C`、`Δt* ≤ 0.02 h`。

| 对照 | max ΔC（最差时空位置） | max ΔT（最差时空位置） | Δt* / h | 状态 |
|---|---|---|---:|---|
| Q1 N800→1600 | 1.89906494e-4（1 s，r=2 cm） | 1.92174390e-4（1500 s，r=2 cm） | — | 通过 |
| Q1 候选→紧容差时间参考 | 2.51087006e-7（2 s，r=2 cm） | 2.03199379e-4（1020 s，r=2 cm） | — | 通过 |
| Q23 N800→1600 | 1.54881207e-4（1 s，r=2 cm） | 2.25045145e-4（960 s，r=2 cm） | 2.22628448e-5 | 通过 |
| Q23 候选→紧容差时间参考 | 3.50895836e-7（11161 s，r=2 cm） | 2.27408475e-4（5460 s，r=2 cm） | 3.53187900e-7 | 通过 |
| Q4 N800→1600 | 2.19411423e-4（124620 s，r=1.2 cm） | 2.62441170e-4（4620 s，表面） | 2.21064022e-5 | 通过 |
| Q4 候选→紧容差时间参考 | 6.51698755e-7（7140 s，表面） | 2.48929782e-4（4620 s，表面） | 1.35335407e-6 | 通过 |

Q4 的固定 `r=1.2 cm` 已单列回归：空间对照最大差 2.19411423e-4（124620 s），时间对照最大差 6.28698911e-8（7200 s），没有用时长收敛替代该列检查。

## 守恒、事件与几何

| 检查 | Q23 | Q4 | 状态 |
|---|---:|---:|---|
| BDF 增广态相对收支残差 | 1.21906842e-15 | 1.91567894e-15 | 通过 |
| 独立 Simpson 求积相对差（最终 0.46875 s） | 4.14348174e-8 | 4.48083537e-8 | 通过 |
| 求积最后一级加密差 | 4.58897374e-8 | 7.14487804e-8 | 通过 |
| 独立时间加密累计 I 相对差 | 2.16155996e-10 | 7.97836650e-10 | 通过 |
| 事件 δC | 2.33447425e-8 | 4.08647446e-8 | 通过 |
| 严格合格分钟样本 | 206940 s，Cmax=0.1499902434 | 183960 s，Cmax=0.1499852689 | 通过 |

V-10 静态极限相对最大差为 3.56216732e-8；V-13 的源半径、`1/R²`、表面对流 `1/R`、通量因子、外推常值及标记检查误差均为0或布尔通过。

BE 诊断记录如实保留：V-4b `dt=1 s` 相对差 1.64264117e-4（失败），`dt=0.25 s` 为 4.10667744e-5（通过），观测阶约1；两档 V-4c 热残差均通过。

## 灵敏度实测

Q23 基线 N400 候选/加密时长差为 2.35650504e-8 h。S1–S6 共10个情景的候选/加密差均同向，且加密后的情景效应大于基线与情景两次求解的合成不确定性；最大合成不确定性为 8.75584924e-7 h。实际启用范围仅 Q23；Q1/Q4 明确记未运行。

## 状态分组

- 已修复并实测通过：F01–F07；候选门控所列 Q1/Q23/Q4 数值检查；Q23 S1–S6；115 项自动化测试。
- 仍失败：仅诊断性 V-4b/BE `dt=1 s` 粗步，未被提升为通过；细步及候选 BDF 通过。
- 局部执行：V-11 附加的 harmonic/integral 时长网格趋势；D12 必需的8/16点全输出对象求积对照已通过。
- 仍未执行：V-7、V-8、V-9、V-12、V-15；正式工作簿尚不存在；灵敏度 Q1/Q4 未运行。
- 待用户授权：D12 具体分问生产配置。生产实现与正式文件生成是后续工作，不在本次返修中冒充完成。

## 复现与自动化验证

```powershell
$env:PYTHONPATH='src'
python -m drymodel.run_all
python -m pytest -q
python 建模方案v1.1/config_check.py 建模方案v1.1/A题_config.yaml
python -O 建模方案v1.1/config_check.py 建模方案v1.1/A题_config.yaml
```

提交前最终回归：115 项测试全部通过；配置检查在普通与 `python -O` 模式均通过。
