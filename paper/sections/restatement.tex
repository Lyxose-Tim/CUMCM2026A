\section{问题重述}

某药材可视为半径 $R_0=\SI{2}{cm}$、长 $L=\SI{25}{cm}$ 的圆柱，初始温度 $T_0=\SI{28}{\celsius}$、
初始干基含水率 $C_0=2.55$。置于热风烘房中烘干，烘房温度 $T_{\mathrm{air}}(t)$ 与「水分浓度」$C_{\mathrm{env}}(t)$
由附件 1 给出（$0$--$\SI{14400}{s}$，$\SI{60}{s}$ 步长）。要求：

\begin{enumerate}[label=\textbf{问题\arabic*.}]
\item 按附录 2 参数，求 $0$--$\SI{1800}{s}$ 内药材温度场与含水率场，给出表 1/2 与 result1（每 \SI{1}{s}、每 \SI{0.1}{cm}）。
\item 按附录 3 参数求整个烘干过程，给出表 3/4 与 result2。
\item 判据「各处含水率 $C<0.15$」，求达标所需时长（单位 h），给出表 5 与 result3（每 \SI{60}{s}）。
\item 药材尺寸随烘干变化（半径按附件 2），按附录 4 参数重解，给出表 6 与 result4。
\end{enumerate}

题面另给对流换热系数 $h=\SI{25}{W/(m^2.K)}$、传质系数 $h_m=\SI{8e-7}{m/s}$（仅附录 2 给出），
扩散系数 $D$ 中温度以 K 计；全部结果保留四位小数。附件 3 给出 result1--4 模板。
